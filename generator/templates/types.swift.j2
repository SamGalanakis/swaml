// Generated by BAML - do not edit
// swift-format-ignore-file

import Foundation
import SWAML

// MARK: - Enums
{% for e in enums %}

{% if e.doc.is_some() %}
/// {{ e.doc.as_ref().unwrap() }}
{% endif %}
public enum {{ e.name }}: String, Codable, Sendable, CaseIterable {
{% for value in e.values %}
{% if value.doc.is_some() %}
    /// {{ value.doc.as_ref().unwrap() }}
{% endif %}
{% if value.alias.is_some() %}
    case {{ value.name|lower }} = "{{ value.alias.as_ref().unwrap() }}"
{% else %}
    case {{ value.name|lower }}
{% endif %}
{% endfor %}
}
{% endfor %}

// MARK: - Classes
{% for class in classes %}

{% if class.doc.is_some() %}
/// {{ class.doc.as_ref().unwrap() }}
{% endif %}
public struct {{ class.name }}: Codable, Sendable, Equatable {
{% for field in class.fields %}
{% if field.doc.is_some() %}
    /// {{ field.doc.as_ref().unwrap() }}
{% endif %}
    public let {{ field.name }}: {{ field.field_type|swift_type }}
{% endfor %}
{% if class.fields.iter().any(|f| f.name.contains('_')) %}

    private enum CodingKeys: String, CodingKey {
{% for field in class.fields %}
{% if field.name.contains('_') %}
        case {{ field.name|camel_case }} = "{{ field.name }}"
{% else %}
        case {{ field.name }}
{% endif %}
{% endfor %}
    }
{% endif %}
}
{% endfor %}
